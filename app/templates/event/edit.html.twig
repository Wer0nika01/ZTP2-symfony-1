{% include 'default/_form.html.twig' with {
    page_title: 'title.event_edit'|trans({'%id%': event.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'event_index',
} %}